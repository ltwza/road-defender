# ── 临时产物 ──
# 约定：仓库根目录下以 `_` 开头的文件一律是"用完就扔"的草稿与输出，
# 不参与版本管理。两类都会命中：
#   · 测试报告（含大量环境相关的耗时数字，每次跑都覆盖，提交进去只污染 diff）
#   · 一次性的辅助脚本（推送/发版/抓取这类，跑完即弃，不该长在仓库里）
# dev/ 下的脚本不在此列 —— 那是长期维护的验证工具，要提交。
# 不带斜杠的模式会在任意层级匹配，所以这一条同时覆盖根目录和 dev/ 下。
_*

# ── 调景期的对照截图 ──
# before / after / after_mid 是当初判断"路两侧该不该加景、加得对不对"时拍的对照图，
# 结论已经落进代码了，留在仓库里只会让人以为是当前效果。展示用图见 hud*.png。
dev/shots/before.png
dev/shots/after.png
dev/shots/after_mid.png

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── 依赖与日志（本项目零依赖，但防止误装后提交） ──
node_modules/
*.log
npm-debug.log*
