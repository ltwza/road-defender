# 统一换行符：仓库里一律存 LF。
# 不固定的话，Windows 上 clone 会把 LF 换成 CRLF，同一个人换个系统再提交就会
# 出现"整个文件都变了"的假 diff —— 对一个几百行的单文件游戏来说，这非常干扰。
* text=auto eol=lf

# 显式声明二进制：图片不能被"换行符规范化"碰，否则字节被改写、文件损坏。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
